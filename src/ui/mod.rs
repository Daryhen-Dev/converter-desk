//! UI widget modules for the converter-desk egui GUI.
//!
//! Declared only in `main.rs` (bin side). The library crate never sees this.

pub mod download_form;
pub mod preview_panel;
pub mod status_view;
