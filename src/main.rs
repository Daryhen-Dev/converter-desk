// Headless stub for PR-A (mvp-shell-headless).
// PR-B will replace this with the full composition root (eframe::run_native + GUI).
fn main() {
    println!("converter-desk — headless stub (PR-A)");
}
